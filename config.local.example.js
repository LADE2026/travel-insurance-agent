// LOCAL OVERRIDES — never committed to git
// Copy this file as-is; only edit your local copy.
// It is loaded AFTER config.js and overrides CONFIG values.

CONFIG.ANTHROPIC_API_KEY = 'YOUR_ANTHROPIC_API_KEY'; // ← replace this locally
